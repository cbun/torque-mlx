# Work Items

Work items are stored under `work/items/<id>/` with durable artifacts (intent/scope/spec/patch/proof).

The append-only coordination log is:

- `work/events.jsonl`

This keeps the coding process replayable and debuggable.

